# Kamiwaza Extensions Configuration
# Copy this file to .env and update with your values

# Kamiwaza API Configuration
KAMIWAZA_API_URL=https://localhost/api
KAMIWAZA_USERNAME=admin
KAMIWAZA_PASSWORD=kamiwaza

# Environment URLs (adjust per tenant/customer)
# For kamiwaza-internal these have been:
#   dev-info.kamiwaza.ai, stage-info.kamiwaza.ai, info.kamiwaza.ai
# Override these to your own dev/stage/prod hostnames when forking this template.
KAMIWAZA_DEV_URL=https://dev-info.kamiwaza.ai
KAMIWAZA_STAGE_URL=https://stage-info.kamiwaza.ai
KAMIWAZA_PROD_URL=https://info.kamiwaza.ai

# Docker Registry Configuration (for publishing images)
# DOCKER_REGISTRY=registry.example.com
# DOCKER_REGISTRY_USERNAME=your-username
# DOCKER_REGISTRY_PASSWORD=your-password

# Extension Registry Publishing (S3/R2)
# Used by: make publish-registry
#
# Per-stage configuration using AWS CLI profiles (required):
#   1. Create profiles in ~/.aws/credentials:
#      [kamiwaza-registry-dev]
#      aws_access_key_id = your-dev-key
#      aws_secret_access_key = your-dev-secret
#
#      [kamiwaza-registry-stage]
#      aws_access_key_id = your-stage-key
#      aws_secret_access_key = your-stage-secret
#
#      [kamiwaza-registry-prod]
#      aws_access_key_id = your-prod-key
#      aws_secret_access_key = your-prod-secret
#
#   2. Set per-stage profile names in .env:
#      AWS_PROFILE_DEV=kamiwaza-registry-dev
#      AWS_PROFILE_STAGE=kamiwaza-registry-stage
#      AWS_PROFILE_PROD=kamiwaza-registry-prod
#
#   3. Publish using STAGE only:
#      make publish-registry STAGE=dev
#      make publish-registry STAGE=stage
#      make publish-registry STAGE=prod
#
# R2 endpoint (same for all stages)
# KAMIWAZA_REGISTRY_ENDPOINT=https://9dc339b60456bbae491b604dc2ece8a4.r2.cloudflarestorage.com
# KAMIWAZA_REGISTRY_REGION=auto
# UPLOAD_METHOD=r2
#
# Per-stage bucket names
# KAMIWAZA_REGISTRY_BUCKET_DEV=dev-info
# KAMIWAZA_REGISTRY_BUCKET_STAGE=stage-info
# KAMIWAZA_REGISTRY_BUCKET_PROD=info

# Per-stage AWS CLI profile names (required for publish-registry)
# AWS_PROFILE_DEV=kamiwaza-registry-dev
# AWS_PROFILE_STAGE=kamiwaza-registry-stage
# AWS_PROFILE_PROD=kamiwaza-registry-prod

# OpenAI Configuration (for extensions that use OpenAI)
# OPENAI_API_KEY=your-openai-api-key

# Development Options
# VERBOSE=true
# DEBUG=true
